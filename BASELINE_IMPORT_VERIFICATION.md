# 客户 Excel 基线导入：验证记录

## 已实现行为

1. 浏览器本地读取 `.xlsx`、`.xls` 和 `.csv` 文件；文件不上传至应用服务端。
2. 自动识别 `Driver ID` / `Value Driver` 与 `Annual Benefit (USD)`，将匹配的年度价值写入 ROI 工作区并自动选中相应 driver。
3. 自动识别 `Assumption` / `Metric` 与 `Value`，对一次性实施成本和年度运营成本进行预填。
4. 无法匹配的价值行、未知列结构和无法读取的文件会在导入复核区提示。
5. 提供含 `Driver Baseline` 和 `Cost Assumptions` 两个工作表的空白模板，供客户数据准备使用。

## 已验证

- TypeScript 检查通过。
- 生产构建通过。
- ROI 导出工作区中的选择、数值重算、PDF 导出和 Excel 导出在浏览器中已验证。
- 导入入口、字段说明和模板下载入口在浏览器界面中已验证可见。

## 注意

浏览器自动化环境未提供本地文件选择器的上传接口，因此实际客户工作簿的选择与导入应由用户在页面中点击“选择客户 Excel”后完成。导入解析逻辑与受支持字段已在前端组件中实现，并由类型检查和生产构建覆盖。
